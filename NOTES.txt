
TODO:
    ✓ load _
    ✓ load BB
    ✓ set up sync handler
    ✓ write server endpoint
    - expose locally, too (at pheme.bast)
        - this needs to be configurable


LOAD:
    ✓ #fetch user

LOGIN:
    - load specific layout with just one widget
    - /api/login
    - if successful, go to fetch user

FETCH USER:
    ✓ /api/user
    - if none: #login
    - get layout
    - if none: default layout
    - instantiate layout
    - instantiate widgets
